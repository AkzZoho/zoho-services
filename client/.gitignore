# Dependencies
node_modules/
**/node_modules/

# Build output (dist is committed so Catalyst can serve the frontend without a build step)
# client/dist/   <-- intentionally NOT ignored; Catalyst reads from client/dist
client/build/

# Env & secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key

# Catalyst local
.catalyst/
.catalystrc.local

# OS / editor
.DS_Store
.vscode/
.idea/
*.log
npm-debug.log*
